# ============================================
# Backend API Configuration
# ============================================
# URL of your Spring Boot backend API
# Default: http://localhost:8080 (if running locally)
NEXT_PUBLIC_API_URL=http://localhost:8080

# ============================================
# Keycloak Authentication Configuration
# ============================================
# Keycloak server URL
# Default for local Docker: http://localhost:8081
# Default for Docker Compose: http://keycloak:8080
NEXT_PUBLIC_KEYCLOAK_URL=http://localhost:8081

# Keycloak realm name (from backend config: reporting-realm)
NEXT_PUBLIC_KEYCLOAK_REALM=reporting-realm

# Keycloak client ID for the frontend
# From backend config: field-masking-interface
NEXT_PUBLIC_KEYCLOAK_CLIENT_ID=field-masking-interface

# ============================================
# Optional: Development Settings
# ============================================
# Enable debug logging (set to 'true' for development)
NEXT_PUBLIC_DEBUG=false

# API request timeout in milliseconds (default: 30000)
NEXT_PUBLIC_API_TIMEOUT=30000
